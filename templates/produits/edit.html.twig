{% extends 'base.html.twig' %}

{% block title %}
	Modifier
{% endblock %}

{% block body %}
	<div class="container">
		<h1>Modifier
			{{ produit.nom }}</h1>

		{{ include('produits/_form.html.twig', {'button_label': 'Modifier'}) }}

		<a class="btn btn-info mt-3" href="{{ path('produits_index') }}">
            retour
        </a>

		{{ include('produits/_delete_form.html.twig') }}
	</div>
{% endblock %}
